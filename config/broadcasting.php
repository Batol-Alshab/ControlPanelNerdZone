<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Broadcaster
    |--------------------------------------------------------------------------
    |
    | يمكنك تحديد الوسيط الافتراضي للبث. هنا نستخدم 'pusher'.
    |
    */

    'default' => env('BROADCAST_DRIVER', 'pusher'),

    /*
    |--------------------------------------------------------------------------
    | Broadcasters
    |--------------------------------------------------------------------------
    |
    | هنا تُعرّف جميع الخيارات الخاصة بمذيعات البث.
    |
    */

    'connections' => [

        'pusher' => [
            'driver' => 'pusher',
            'key' => env('PUSHER_APP_KEY'),
            'secret' => env('PUSHER_APP_SECRET'),
            'app_id' => env('PUSHER_APP_ID'),
            'options' => [
                'cluster' => env('PUSHER_APP_CLUSTER'), // مثال: 'mt1'
                'useTLS' => true,
            ],
        ],

        // يمكنك إضافة جهات اتصال أخرى هنا إذا أردت، مثل 'redis' أو 'log'.
    ],

];